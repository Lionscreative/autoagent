Create a professional brand identity for "NorthPeak Ventures", a VC firm.

Requirements:
- Customize the site favicon (app/icon.tsx) with the letter "N" in a dark navy gradient
- Customize the Apple touch icon (app/apple-icon.tsx) with the same brand style
- Update the site title and description in metadata to match NorthPeak Ventures
- Make sure icon.tsx and apple-icon.tsx build successfully (no prerender errors)
- The project is at /project — modify files there
- Run `npm run build` to verify everything compiles
