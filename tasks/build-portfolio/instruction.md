Create a professional portfolio website for a freelance photographer named "Sarah Chen".

The website should have:

1. **Homepage** (`app/page.tsx`) with:
   - A hero section with her name and tagline "Capturing moments that matter"
   - A grid gallery section showing 6 placeholder images (use colored div placeholders)
   - A brief about section

2. **About page** (`app/about/page.tsx`) with:
   - A longer bio paragraph
   - A list of services (Wedding, Portrait, Event, Product photography)
   - Contact information (email: sarah@example.com)

3. **Contact page** (`app/contact/page.tsx`) with:
   - A contact form with fields: Name, Email, Message
   - The form doesn't need to submit anywhere, just the UI

Requirements:
- Use Tailwind CSS for all styling
- Dark, elegant color scheme (dark backgrounds, gold/amber accents)
- Responsive design (works on mobile and desktop)
- The project is already set up at /project — modify files there
- Run `npm run build` to verify everything compiles
