Create a todo app with categories. I want to add tasks with a title and category (Work, Personal, Shopping). Show tasks grouped by category. Let me mark them as done and delete them. Dark theme.

Requirements:
- Use Tailwind CSS for styling
- All state managed client-side (no database needed)
- The project is at /project — modify files there
- Run `npm run build` to verify everything compiles
