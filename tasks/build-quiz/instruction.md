Make an interactive quiz app about world capitals. 10 questions, multiple choice (4 options each). Show score at the end with correct/incorrect breakdown. Add a "Play Again" button.

Requirements:
- Use Tailwind CSS for styling
- All logic client-side (questions hardcoded in the app)
- The project is at /project — modify files there
- Run `npm run build` to verify everything compiles
