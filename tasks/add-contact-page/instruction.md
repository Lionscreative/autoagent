The project at /project already has a working homepage. Add a contact page WITHOUT modifying the existing homepage.

Create:
1. A contact page at `app/contact/page.tsx` with:
   - A heading "Get in Touch"
   - A form with: Name, Email, Phone, Message fields
   - A submit button styled with Tailwind
   - Company info: "Acme Corp, 123 Business Ave, New York, NY 10001"
   - Email: contact@acme.com, Phone: (555) 123-4567

2. Add a link to the contact page in the existing navigation (if nav exists in layout.tsx)

IMPORTANT: Do NOT modify or rewrite `app/page.tsx`. Only add new files and make minimal edits to layout.tsx if needed.

Run `npm run build` to verify everything compiles.
