Build an expense tracker. I can add expenses with: amount, category (Food, Transport, Entertainment, Bills, Other), and date. Show a summary with total spent and breakdown by category. Include a bar chart or visual.

Requirements:
- Use Tailwind CSS for styling
- All state managed client-side (no database needed)
- The project is at /project — modify files there
- Run `npm run build` to verify everything compiles
