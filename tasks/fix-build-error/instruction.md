The Next.js project at /project has build errors. Fix them so that `npm run build` passes.

Do NOT rewrite files from scratch — fix the existing code. Read the error messages
from `npm run build`, identify the issues, and make targeted fixes.

The project should have a working homepage when you're done.
