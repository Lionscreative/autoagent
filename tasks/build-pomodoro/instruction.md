Créer un timer Pomodoro. 25 minutes de travail, 5 minutes de pause. Afficher le temps restant en gros. Boutons Start, Pause, Reset. Compteur de sessions complétées. Design minimaliste.

Requirements:
- Use Tailwind CSS for styling
- All logic client-side
- The project is at /project — modify files there
- Run `npm run build` to verify everything compiles
