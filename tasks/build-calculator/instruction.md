Build me a mortgage calculator. User enters: home price, down payment %, interest rate, loan term (15 or 30 years). Show monthly payment, total interest paid, and an amortization breakdown. Clean modern UI.

Requirements:
- Use Tailwind CSS for styling
- All logic should work client-side (no API calls needed)
- The project is at /project — modify files there
- Run `npm run build` to verify everything compiles
